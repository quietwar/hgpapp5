# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
  # api_key: AIzaSyB49uC_ZAuE5ef0ouy5DuVJ1qroP7qN6Ss

# Environmental secrets are only available for that specific environment.
development:
  admin_name: Hodari_Toure
  admin_email: hodari@hiddengeniusproject.org
  admin_password: quiet927
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  domain_name: hiddengeniusproject.org

  secret_key_base: <%= Figaro.env.secret_key_base %>
  api_key: 'AIzaSyB49uC_ZAuE5ef0ouy5DuVJ1qroP7qN6Ss'
  google_client_id: 542001295987-1a2tcq6vm4ndsov68svt3e1379lpetnk.apps.googleusercontent.com
  google_client_secret: tDdFW_ZGEoSYyn8o5PpqScGJ

test:
  secret_key_base: <%= Figaro.env.secret_key_base %>
  domain_name: hiddengeniusproject.org

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted  secrets
# and move the `production:` environment over there.

production:
  admin_name: <%= ENV["ADMIN_NAME"] %>
  admin_email: <%= ENV["ADMIN_EMAIL"] %>
  admin_password: <%= ENV["ADMIN_PASSWORD"] %>
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  secret_key_base: <%= Figaro.env.secret_key_base %>
